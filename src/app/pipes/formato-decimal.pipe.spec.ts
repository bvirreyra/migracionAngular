import { FormatoDecimalPipe } from './formato-decimal.pipe';

describe('FormatoDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
