import { KzMaskDirectiveDirective } from './kz-mask-directive.directive';

describe('KzMaskDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new KzMaskDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
