import { PipeTranformaPipe } from './pipe-tranforma.pipe';

describe('PipeTranformaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTranformaPipe();
    expect(pipe).toBeTruthy();
  });
});
